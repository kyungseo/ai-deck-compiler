---
description: "blueprint.yaml 초안 생성 — /create-deck으로 통합됨"
argument-hint: "[발표 주제 — /create-deck 사용 권장]"
disable-model-invocation: true
---

> **이 커맨드의 기능은 `/create-deck`에 통합되었습니다.**
> blueprint 작성 규칙(Narrative Spine, Action Title, 슬라이드 타입별 YAML)이 `skills/create-deck.md` Step 2~3에 포함되어 있습니다.
>
> 새 deck을 처음부터 만들거나 blueprint를 작성하려면 `/create-deck`을 사용하세요.

`skills/create-deck.md`를 로드해서 Step 0부터 절차를 따라줘.

`$ARGUMENTS`가 있으면 발표 주제 힌트로 활용한다.
